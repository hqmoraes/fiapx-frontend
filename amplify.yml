version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting FIAP-X build with fixed IAM role"
    build:
      commands:
        - echo "Building FIAP-X Frontend"
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
